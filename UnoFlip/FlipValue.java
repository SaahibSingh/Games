public enum FlipValue {
    ZERO, ONE, TWO, THREE, FOUR, FIVE, SIX, SEVEN, EIGHT, NINE,

    // Light side special cards
    LIGHT_SKIP,
    LIGHT_REVERSE,
    LIGHT_DRAW_ONE,
    LIGHT_FLIP,
    LIGHT_WILD,
    LIGHT_WILD_DRAW_TWO,

    // Dark side special cards
    DARK_SKIP,
    DARK_REVERSE,
    DARK_DRAW_FIVE,
    DARK_SKIP_EVERYONE,
    DARK_FLIP,
    DARK_WILD,
    DARK_WILD_DRAW_COLOR
}
